<Window x:Class="Notebook.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:ci="clr-namespace:CommonInformation;assembly=CommonInformation"
        xmlns:notebook="clr-namespace:Notebook"
        Title="Ежедневник" Height="350" Width="525" MinWidth="300" Background="{x:Null}"
        WindowStyle="None">
    <Window.DataContext>
        <notebook:NotesViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <ResourceDictionary Source="GenericResources.xaml"/>
    </Window.Resources>
    <Border BorderBrush="Black" BorderThickness="1" Background="{StaticResource topBackgroundBrush}"
            CornerRadius="5,5,5,5" MouseDown="Border_MouseDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <tb:TaskbarIcon Name="TaskbarIcon" Grid.Row="0" IconSource="{Binding Source={x:Static ci:Images.TrayIconKey}}" 
                            ContextMenu="{StaticResource TrayMenu}" MenuActivation="RightClick"/>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0" Source="{Binding Source={x:Static ci:Images.LeftArrowImage}}"/>
                <TextBlock HorizontalAlignment="Center" Grid.Column="1" FontSize="20"
                           Margin="30,5,30,5">11 июня 2017</TextBlock>
                <Image Grid.Column="2" Source="{Binding Source={x:Static ci:Images.LeftArrowImage}}">
                    <Image.LayoutTransform>
                        <RotateTransform Angle="180"/>
                    </Image.LayoutTransform>
                </Image>
            </Grid>
            <ListBox Grid.Row="1" ItemsSource="{Binding Notes}" Style="{StaticResource listboxNotesStyle}"/>
        </Grid>
    </Border>
</Window>
