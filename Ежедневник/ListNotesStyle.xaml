<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:models="clr-namespace:DatabaseController.Models;assembly=DatabaseController"
                    xmlns:notebook="clr-namespace:Notebook"
                    xmlns:commonInformation="clr-namespace:CommonInformation;assembly=CommonInformation">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/CommonInformation;component/GenericResources.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <Style TargetType="Button" x:Key="archivedIndicatorStyle">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}" Cursor="{TemplateBinding Cursor}"
                            Margin="{TemplateBinding Margin}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0"/>
                            <Image Name="Icon" Grid.Column="1" Source="{Binding Source={x:Static commonInformation:Images.ArchiveImage}}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Icon" Property="Source" 
                                    Value="{Binding Source={x:Static commonInformation:Images.ArchiveActivatedImage}}"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, 
                            Path=DataContext.(notebook:NotesViewModel.ViewArchived)}" Value="true">
                            <Setter Property="ToolTip" Value="{commonInformation:LanguageHelper Key=MoveFromArchive}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, 
                            Path=DataContext.(notebook:NotesViewModel.ViewArchived)}" Value="false">
                            <Setter Property="ToolTip" Value="{commonInformation:LanguageHelper Key=MoveToArchive}"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>
    <Style x:Key="noteListBoxStyle" TargetType="ListBox">
        <Setter Property="TextBlock.FontSize" Value="14"/>
        <Setter Property="TextBlock.FontFamily" Value="Calibri"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
        <!--<Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border CornerRadius="2" 
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer x:Name="ScrollViewer" Padding="{TemplateBinding Padding}" HorizontalScrollBarVisibility="Disabled"
                                      CanContentScroll="False"
                                      Background="{TemplateBinding Background}">
                            <ItemsPresenter MaxWidth="{Binding ElementName=ScrollViewer, Path=Content}"/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>-->
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Background" Value="{StaticResource noteBackgroundBrush}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                <Border x:Name="brd" Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="3" CornerRadius="4">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="BorderBrush" Value="LimeGreen"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Background" Value="{StaticResource topBackgroundBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>

        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate DataType="{x:Type models:Note}">
                    <Grid Name="NoteContainer">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" >
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="{Binding Date, StringFormat={x:Static models:Note.DataFormat}}" />
                            <TextBlock Grid.Row="1" Text="{Binding Message}" TextWrapping="Wrap" Margin="0,0,5,0"/>
                        </Grid>
                        <ContentPresenter Grid.Column="1" Visibility="Collapsed"
                                          Content="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, Path=DataContext}"/>
                        <Button Grid.Column="1" Width="32" Height="32" Style="{StaticResource archivedIndicatorStyle}"
                                Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, 
                            Path=DataContext.(notebook:NotesViewModel.MoveToArchveCommand)}" CommandParameter="{Binding}">
                        </Button>
                    </Grid>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding Priority}" Value="Low">
                            <Setter TargetName="NoteContainer" Property="Background" Value="{StaticResource lowPriorityBrush}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Priority}" Value="Medium">
                            <Setter TargetName="NoteContainer" Property="Background" Value="{StaticResource mediumPriorityBrush}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Priority}" Value="Hight">
                            <Setter TargetName="NoteContainer" Property="Background" Value="{StaticResource hightPriorityBrush}"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="VerticalAlignment" Value="Top" />
    </Style>
    
</ResourceDictionary>